#大部分基于socks协议的主流科学上网客户端，订阅都是反解码基于客户端生成的分享链接(scheme:具体参数)再转成base64，再逐个导入
#所以我们只要将需要加入订阅的节点信息导出，然后转成base64，再将这串base64放置到公网上，导向该串编码的信息就是订阅链接
#最稳妥的方式自然就是自己建站，反正刚好有用到nginx做伪装，简单配置下可以在公网访问就好
#不会操作nginx也可以利用GitHub的“raw”，也可直接访问获取，但必须放到公开仓库不太安全
#最次可用第三方提供的订阅转换相关功能，不建议，风险较大
